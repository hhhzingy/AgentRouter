# V1.1 Windows+Mobile 收口状态（Z0—Z9）

分支:`feat/v1.1-final-windows-mobile` · 基线 `2bd41f9` · 远端 CI:C1 + W11 双绿
最新提交:`0635a64` · 离线全量:419/419 PASS(86 文件) · 敏感扫描:0 findings

## 一句话

V1.1 的**远程协议中枢(Transport/Gateway/Pairing)+ 手机控制台 + 桌面 REMOTE_CORE + Context/DeepSeek 压缩闭环工具**已落地并有离线 conformance / 单元证据;Z7 pi 真实生产 E2E 已 PASS。**尚未**全部通过:Z7 dsh 真实 bootstrap、Z2 ZCode 任务闭环、Z5 双机、Z6 真机、Z8 Tailscale/soak、Z9 最终 RC —— 多数需**用户物理动作或 v1.0 血缘移植**。故**不输出** ACCEPTED / RC_READY。

## 已完成并 CI 验证(提交)

| 阶段 | 内容 | 提交 |
| --- | --- | --- |
| Z0 | 修复远端 C1/W11 FAIL(context-native-compaction 补驱动实测 usage)+ vitest 30s;冻结门禁本地化 | 0a097ba |
| Z2 | ZCode 受管 Role **create 路径打通**:实证定位 `-32603 Model config is missing` 根因(config.json 缺 `model` 键),提取官方 zcode.cjs 确认 `model:{main:'zai/glm-4.6'}`+`provider` 形状;prepareManagedZcodeProfile 写非秘密 model/provider 且容忍 oauth 写入;live 探针 create 真实 PASS(2/2) | 702e076 |
| Z3/Z4 | RemoteWebSocketTransport + RemoteGateway(HTTP /pair HttpOnly Secure cookie + WSS 桥接 ApplicationService;Host/Origin 白名单、maxPayload、限流、撤销关活连接、remote 禁 desktop_directory locality)+ migration 013 设备/配对(仅存哈希)+ RemoteDeviceStore(createPairing/consume/authenticate/revoke) | 9c65abf |
| Z5/Z6 | console.html 手机远程控制台(配对→WSS→概览/需处理/任务/角色/控制;写:acquire/release/WAITING_INPUT/approve/reject/cancel/新任务/WorkSession);Gateway GET / 静态服务+CSP;node-ledger(safeStorage 加密 token,renderer 不碰);p1-main REMOTE_CORE 模式 + remote IPC | 0b6a945, 0635a64 |
| Z1 | DeepSeek ~1M→256K **真实压缩闭环工具**(合成哨兵事实+分段真实调用+语义探针+续办+全指标);按用户指示本轮跳过完整实测,密钥可用、脚本就绪 | 0635a64 |

离线证据:remote-gateway conformance 5/5(REMOTE-01/03/05/06/11 + PAIR-01/02/05/06/07/09)、node-ledger 3/3、profile 5/5、live create 探针 2/2、**Z7 pi 生产 E2E PASS_TASK_AND_BOOTSTRAP(run SUCCEEDED)**。

## 未完成(诚实分母,不伪造)

| 阶段 | 缺口 | 原因/所需 |
| --- | --- | --- |
| Z7 dsh 真实 | `--live --dsh` bootstrap FAILED(stderr 空) | 此分支未带 v1.0 R3 的 ACP 级 DBG/修复血缘;需移植 c1r1p2 响应级 P2 放宽+路由到 memory.ts 或按 v1.0 方法多轮 live 定位。**未通过** |
| Z7 kimi/codex 真实 | 未在本分支跑 | 需对应凭据与 live(账号切换仍 EXCLUDED) |
| Z2 ZCode 任务闭环 | route_finish/PUBLISHED/resume/cancel/A→B→A→C 未跑通 | create 已通,任务轮需**模型认证**(DUT 登录或 Zhipu key)。见操作卡 |
| Z5 双机 | 打包 GUI→Tailscale→第二 Windows Core | 需第二台 PC(hzx-lab 在线)+ 用户操作 + 打包 |
| Z6 真机 | 真实手机写控制 | 需 Tailscale Serve 配置 + 手机,见操作卡 |
| Z8 Tailscale/Serve/soak/恢复 | `tailscale serve` 现无规则;4h soak、双机/手机网络切换 | 需用户授权 Serve HTTPS + 时间窗 |
| Z1 1M 全量度量 | 逻辑~1M 真实压缩指标未采集 | 用户本轮指示跳过;脚本就绪,密钥可用 |
| Z9 最终 RC | 单一干净 SHA 全量 + 打包 + 用户 smoke | 依赖以上全绿 |

## 迁移与冻结纪律

- migration 001—012 字节不变(冻结清单 + CI freeze gate 校验);本轮仅新增 013(remote devices/pairings)。
- 未改任何 C1/C1R1P1/C1R1P2 冻结合同文件;REMOTE_CORE 用既有 C1 帧语义,未造第二套 REST 业务。
- 账号切换:`EXCLUDED_BY_USER`。Linux:`NEXT_PHASE_AFTER_WINDOWS_MOBILE_ACCEPTANCE`(本轮未动)。

## 下一步(需用户,见各 USER_ACTION_CARD)
1) ZCode DUT 登录或提供 Zhipu key;2) 手机/Tailscale Serve;3) 第二 Windows PC 双机;4) 授权 4h soak 窗口。
这些闭合后 Z7/Z2/Z5/Z6/Z8 才能真实转绿,Z9 才成立。
