# 阶段进展

当前为离线开发预览，M00—M09 均有基础工作或部分实现，不能视为阶段全部完成。正式支持的真实 Harness 数为 **0**，所有发布 Gate 保持未通过。

用户已指定先离线开发，真实账号联调后续进行；pi 安装在 Windows 用户目录 `%APPDATA%/npm`。旧项目内测试副本保留但不再用于启动。未读取或复制日常账号凭据。

本次检查：`2026-09-09T07-58-24-884Z`，13/13 组 PASS。应用测试共 43 条（{'unit': 6, 'integration': 18, 'contract': 16, 'chaos': 3}），这是离线测试计数，不是 81 条产品验收通过数。完整命令、退出码、原始日志见 [最新验证](../evidence/latest-run.json)。

## M00

- 已实现：需求追踪、兼容性锁、架构决策、证据目录；版本及无账号握手；私有 Node、SQLite 与 Windows Job 探针。
- 已测试：规范校验、版本探测、数据库、Job、打包及桌面验证通过。
- 被阻断：真实账号联调按用户要求延期；缺少干净 Windows 环境。
- 已知风险/未完成：开发机证据不代表干净机认证；阶段 Gate 尚未全部通过。

## M01

- 已实现：协议验证、领域状态、基线迁移、SQLite WAL/FULL/FK 和事务封装。
- 已测试：非法包、有限策略 DSL、存储与事务用例通过。
- 被阻断：无新增环境阻断；剩余工程可离线继续。
- 已知风险/未完成：完整策略执行、跨表权限审查尚未完成。

## M02

- 已实现：幂等账本、Outbox、FIFO、关联结果续办、等待依赖、HELD 收尾屏障、恢复隔离、配额及六工具内部桥。
- 已测试：静默成功、指定去向、不抄送、结果先到与后到、回滚及恢复用例通过。
- 被阻断：真实生命周期收尾证明需后续联调。
- 已知风险/未完成：独立 RESULT_HANDLING、人工续办/挂起、对账操作、命名资源尚未完成。

## M03

- 已实现：Codex 实机类型与 Schema 固化、初始化探针、终态事件归一化。
- 已测试：无账号初始化及事件契约测试通过。
- 被阻断：真实账号联调延期。
- 已知风险/未完成：完整 HarnessAdapter 未实现；不计真实支持。

## M04

- 已实现：Kimi ACP v1 初始化探针及终态归一化。
- 已测试：协议握手及事件契约测试通过。
- 被阻断：真实账号联调延期。
- 已知风险/未完成：反向 RPC、模型/历史与完整适配器未完成；不计真实支持。

## M05

- 已实现：pi 0.85.1 用户级安装、RPC 分帧、受控扩展源码、agent_settled 终态归一化。
- 已测试：版本、无账号 RPC、分帧和事件契约测试通过。
- 被阻断：真实账号及模型工具联调延期。
- 已知风险/未完成：agent_end 不视作收尾；完整适配器未完成；不计真实支持。

## M06

- 已实现：项目/空间/角色管理服务、绑定未验证状态、不可变文件产物。
- 已测试：项目归档保留源文件、角色管理、产物完整性及作用域用例通过。
- 被阻断：无新增环境阻断。
- 已知风险/未完成：Git 保留引用、工作树、AGENTS 镜像、绑定交接与清理策略未完成。

## M07

- 已实现：桌面项目登记/归档、角色创建/暂停、状态投影、兼容性页、托盘与独立 Core。
- 已测试：Playwright 验证角色创建/暂停、窗口隐藏恢复后 Core PID 不变；沙箱与私有运行时验证通过。
- 被阻断：缺少干净 Windows 安装/卸载条件。
- 已知风险/未完成：本轮使用软件渲染；目录和关闭对话框使用测试替身；完整审批、取消和全部收尾场景未完成。

## M08

- 已实现：SQLite 在线备份及产物校验、注入端口的切号状态机、配额未知/过期状态。
- 已测试：离线备份与切号状态机测试通过。
- 被阻断：真实账号联调延期。
- 已知风险/未完成：未写入真实凭据；完整恢复切换、升级回滚及账号 UI 未完成。

## M09

- 已实现：精确依赖锁、复现步骤、验证脚本、逐命令退出码与日志、便携预览构建。
- 已测试：当前 13 组检查通过，详见本次证据索引。
- 被阻断：真实账号、干净 Windows、断电及长期压力条件缺失。
- 已知风险/未完成：81 条产品验收未完成；状态为 NOT_RELEASED，不作 V1.0 交付。

## W10 / C1

本轮合同冻结与修正详见 [W10 报告](reports/W10-C1.md)。未推进 W11；既有 M00—M09 完整产品验收状态不因合同测试通过而提升。
