# W10 / Client API C1 交付报告

基线提交：`16370d3971740c80ca9ccec7a6d9b3553e896545`。

新提交：`feat/contract-c1` 上包含本报告的 C1 提交；最终回复提供 Git 哈希。状态：**FROZEN_PENDING_REVIEW**。本轮仅 W10，未进入 W11，不创建 UIAI worktree，不启用真实账号。

## 已实现

- 66 个正式客户端方法，严格 JSON-LF 帧、生成 TypeScript/ViewModel、错误码、capability、事件游标、revision、controller lease、幂等和 UNKNOWN 审计合同。
- 唯一 Schema 来源、确定性生成/漂移检查、冻结哈希、UI fixtures、InMemory/stdio Mock、CoreTransport 接口、显式 Mock preload 入口及 UIAI 只读文档。
- route_context 分段最小披露与有效策略；目标角色被动通知分页，持久化但不唤醒。
- AuthUnit V1.0 单并发约束；SQLite 最低安全版本+验证区间+只读诊断策略。
- 字段白名单诊断、原始日志提交禁用、暂存区/历史/指定目录秘密扫描、提交钩子和 CI。历史原件保留，不删除用户文件。

## 测试与证据

最终完整回归：`2026-09-09T11-47-39-478Z`，**15/15 组 PASS**。应用测试共 **60 条**：单元 6、集成 22、合同 29、Chaos 3。不等于原81项产品验收，也不增加真实 Harness 支持数。

主要命令：`node tools/checkpoint.mjs`、`node tools/generate-client-contract.mjs --check`、`node tools/check-client-freeze.mjs`、`node tools/check-sensitive.mjs --staged`、`node tools/check-sensitive.mjs --history`、`node tools/check-sensitive.mjs --tree release/AgentRouter-preview`。

真实进程/系统测试：Windows 开发机、Node24.14.0、SQLite3.53.4、Electron44.3.0；数据库加载、Job 子孙进程停止、便携私有 Core、桌面原有管理回归、C1 preload 开关和调用链、独立 Node stdio Mock；git commit 负面测试确实未产生提交。

模拟测试：租约、重连幂等、事件 catchup、修订/作用域、UNKNOWN 证据/审计/资源处置均为 C1 Mock；没有真实 Harness、真实 SSH、真实账号。现有核心静默成功、显式结果去向、FIFO、关联续办和 HELD 屏障回归通过。

首次完整回归中数据库探针因 Node strip-only 不支持参数属性失败，已修复；旧失败摘要保留，最终批次全部通过。桌面软件渲染；目录/关闭对话框使用已有测试替身。

## G10 逐项结论

| ID     | 状态               | 证据及边界                                                                    |
| ------ | ------------------ | ----------------------------------------------------------------------------- |
| G10-01 | PASS 合同          | Schema/生成类型/Mock/文档；冻结和漂移校验                                     |
| G10-02 | PASS Mock          | 新连接新租约重放同 op 不重复；生产持久账本在 W11                              |
| G10-03 | PASS Mock          | 多 observer 拒写、抢租约/过期拒绝；真实 OS 身份在 W12                         |
| G10-04 | PASS 合同          | git/live/真实模型不可用；未实现方法拒绝                                       |
| G10-05 | PASS 离线          | 白名单脱敏；13个旧 raw log 仅取消跟踪、原件保留                               |
| G10-06 | PASS 离线          | canary 扫描失败、真实 pre-commit 阻断；非 OS 沙箱 Gate                        |
| G10-07 | PASS Core          | 默认身份，各 section 不附带其他段落                                           |
| G10-08 | PASS Core          | 重启后目标可见，分页，无 Outbox/新任务/回执                                   |
| G10-09 | PASS 当前引擎/策略 | 实测3.53.4；未知安全版本只读诊断，不伪造扩展验证范围                          |
| G10-10 | PASS 合同/Mock     | 证据绑定 Run/epoch、幂等审计及模拟处置；真实核验/资源解除未实现，留待 W11/W13 |

这是 C1 交付范围验收，不宣布整个 G10—G13 生产系统通过。

## 未测试、阻断与风险

本轮无环境阻断 C1 交付。真实账号、SSH/Linux 常驻服务、跨连接耐久账本、真实 UNKNOWN 核验/资源解除、Provider 捕获、OS 凭据隔离、网络拒绝、干净 Windows 安装均未验收。SG-2—SG-6 未通过；SG-1 只有明确范围的静态扫描。三家真实支持保持0。

Mock 不应被接为生产服务；生产默认拒绝新 C1 入口，旧桌面 API 保留至后续迁移。新字段或改义走 CCR，不私改冻结哈希。CI 配置已经加入，远端执行状态以 GitHub Actions 为准，不用本地通过冒充远端通过。

## 修改文件与兼容性

合同：`contracts/client-api.c1.schema.json`、`packages/client-contract/**`、`packages/client-transport/**`、`fixtures/client-c1/**`。

实现：`packages/core-api/**`、`packages/security/**`、`packages/runtime/core.ts`、`packages/storage/index.ts`/`compatibility.ts`、Route 工具定义、Electron main/preload 和显式 Mock 接线；未修改 Renderer/组件/样式。

验证治理：新增合同与 W10 测试、生成/扫描/冻结/Mock 工具、钩子、CI；改进证据生成、版本与握手报告脱敏，npm 缓存改为项目相对路径。原始两份执行包原样保留。

详见 [只读接入指南](../api/README.md)、[兼容性表](../api/compatibility.c1.md)、[安全 Gate](../api/security-gates.c1.md)、[冻结清单](../api/freeze.c1.json)、[最新回归](../../evidence/latest-run.json)。

下一步：停止等待用户/复核者确认 C1；确认后才建立 integration/core/ui 分支与项目内 worktree，按 W11 接真实服务。未自行继续。
