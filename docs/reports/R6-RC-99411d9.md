# R6 候选报告——单一干净 SHA `99411d9`

日期:2026-09-14 · 分支:`feat/v1-finalization-j3` · 工作树:`git status --porcelain` 为空(dirty_source=false)

## 结论

`99411d9` 满足本轮全部可自主执行验收:离线全量 418/418、冻结四套(spec/contract/migrations/doctor)、
typecheck/lint、四条真实活体 E2E(pi / Kimi / DeepSeek Harness / DeepSeek A→B→A→C)全部通过。
**合 main、tag、发布继续冻结,等待用户验收批准。**

## 证据矩阵(全部在本 SHA 上执行)

| 项 | 结果 | 证据 |
| --- | --- | --- |
| 全量测试(integration/contract/unit/ui/chaos) | 68 文件 418/418 PASS | `.local` 运行日志;本报告同轮执行 |
| typecheck / lint | OK | `tsc --noEmit`、`tools/lint.mjs` |
| spec:check / contract:check / migrations manifest / doctor | OK/PASS | 冻结门禁原样输出,敏感扫描 PASS(1949 文件 0 findings) |
| pi 生产 E2E | PASS_TASK_AND_BOOTSTRAP,42 PUBLISHED | `.local/rc-pi.log` |
| Kimi 生产 E2E | PASS(第 2 次;第 1 次为已记载的模型行为波动,Core 保守呈现不伪造) | `.local/rc-kimi.log`、`.local/rc-kimi2.log` |
| DeepSeek Harness 生产 E2E(`--dsh`) | PASS_TASK_AND_BOOTSTRAP,42 PUBLISHED | `.local/rc-dsh.log` |
| DeepSeek A→B→A→C(`--dsh --ab`) | PASS:3 会话 / 3 交接包全 ACKED / 3 个互异原生会话引用,4 run 全 SUCCEEDED | `.local/rc-dsh-ab.log` |

## 本轮(续12—13)落地的核心修复/特性

- **R3**:dsh `saveSession` 落地;C1R1P2 放宽同时覆盖冻结 schema 的 `enum` 与 `anyOf[{const}]` 两种 harness 形态;
  P2 连接的帧/结果校验走放宽 schema;旧协议连接投影裁剪扩展到 runs/role.list/run.list;dsh runPrompt 作废
  bootstrap 一次性 ACK 指令(弱模型复读致任务轮空转)。
- **R4**:migration 009 `role_session_handoffs`;原生会话引用按 RoleSession 键控(仅初始会话继承 bootstrap 的
  binding 级引用);交接包在 create/switch 时生成,目标会话首运行须先 ACK(ACK 前 Route 工具拒绝),
  ACK 文本经 `handoff_ack` 帧推进为 ACKED;GUI RolePage 工作会话卡(列表/新建/切换/观察者只读)。
- **R5**:migration 010 `runs.execution_provenance` + `execution_profiles.fallback_json`;run 终态由 Core 写
  溯源(harness/model/outcome/diagnostic);Kimi→DeepSeek 显式降级为操作者登记策略,命中诊断码时写入触发态并
  阻断派发,**不静默重派**;ssh-bridge 早到帧排队回放修复;ssh-bridge 进入 w11-core 构建分发。
- **R6**:p1 stdio 用例超时提至 30s(esbuild+子进程在全量并行下超默认 5s,单跑 3.4s),消除唯一已知 flake。

## 待用户动作(不阻塞本 SHA 的验收结论,阻塞对应活体项的闭环)

1. **SSH 真实 E2E**(工具已就绪 `tools/test-ssh-production.mjs --live`):需提权执行一次
   `powershell -NoProfile -ExecutionPolicy Bypass -File .local/ssh-e2e/install-auth.ps1`
   (向 administrators_authorized_keys 追加带标记的 forced-command 行,幂等;不触碰既有键),
   然后重跑测试即可完成 真实sshd→桥→Core 命名管道 的观察者读+写拒绝验收。
2. **ZCODE_DUT**:本机为 ZCode 桌面版(E:\software\ZCode),无独立 `zcode` CLI;驱动/生命周期/注册表已实现并
   单测覆盖,待 CLI 安装并登录后按 0.16.5 版本口径接入。
3. **真机控制台**:Tailscale + 手机访问,用户操作。
4. **合 main / tag / 发布**:按约定继续冻结,等待用户对候选 SHA 的验收批准。

## 已知限制(摘自 known-limitations,本轮未恶化)

- Kimi K2.7 在 session/load 后偶发不执行任务指令(重答章程 ACK 或漏发);Core 一律保守呈现,不伪造结果,
  不自动重放——本轮 Kimi 首跑复现一次,复跑通过,行为与记载一致。
- Kimi 5 小时配额窗口影响活体验收时机;显式降级策略已可经 `setProfileFallback` 登记。
- Windows Job helper 的干净账户/卸载/断电/长期压力验收仍未做;安装卸载与系统退出不在本 SHA 声称范围。
