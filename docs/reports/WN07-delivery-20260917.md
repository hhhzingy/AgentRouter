# WN07 交付报告（2026-09-17，本轮 MCP 与集成执行包）

固定输入:Windows `d99cb10`(含 SC1/SC2)× Linux `0645f20` → 组合 `6e95922`(integration/v1.1-cross-platform,已推送)。
组合树本机验证:tsc 干净、四套 **474/474**(33 平台跳过)、pre-commit 三闸 findings=0。
候选包(同一组合 SHA,manifest sourceDirty=false,双模式 LOCAL+REMOTE opt-in,含 console/两 MCP 入口/cloud-kit):
`E:\AgentRouter\.worktrees\v1.1-integration\release\AgentRouter-j3-6e95922e6675-3725753a-*`。

## 首屏:三类尚缺(不以“不属于本包”逃避)

### A. 尚缺代码(本轮未落地,已定位)
1. 外接参与者 **claim/result_submit 共享合同**(迁移槽 016 预留):有界认领、防迟到提交、
   结果登记与显式下游交付;不给网页角色伪造 Run/Job 终态。当前 participant 具备
   grant 复验的读、幂等产物、WAITING_INPUT 补输入,但“复核结果→下游续办”未闭环。
2. **ZCode warm 会话驻留 backend**:协议已证同进程两轮连续(BANANA-7);受审 backend 仍是
   每 Run 释放进程(树空停止证据)。需要会话 host 与 Run 工具负载分层设计;
   在完成前产品面为显式 SESSION_CONTINUATION_UNSUPPORTED+新 WS 指引(诚实,非缺失)。
3. **ContextTransfer 端口的真实运行验证**:zcode 端口已注册且按 operationId 幂等,但一次真实
   inherit 全程(导出→压缩通道→目标→提交→第二轮依赖)尚无 live 记录;跨 Harness 继承的
   binding 切换代码在组合树,同样待真实跑。
4. Linux 采纳共享 StderrRing 后删除其 host 内联副本(组合树暂双留,均绿)。

### B. 尚缺宿主能力/环境
1. GitHub Actions **计费/消费上限**(BLOCKED_CI_INFRA):两条车道新作业均未启动(steps=[]);
   组合 SHA 的远端双门+Linux 三门**尚未在 CI 实际运行**——本机验证不能替代。
2. 官方 Secure MCP Tunnel 客户端:需操作员核验来源/版本/hash 后安装(本包不代装);
   ChatGPT 侧连接器能力(工具列表渲染、文本通道)以实测为准。
3. 手机/第二机跨设备(WSS/HTTPS):按用户 2026-09-17 指示本轮**明确不执行**,卡保留。

### C. 实际人工待办(最少动作)
1. WN05 演练 A/B:`docs/cloud-kit/USER_ACTION_CARD-web-mcp.md`(OpenAI 账号关联、
   网页主动发一条调用、web-ready.json 回填)——你已同意配合,随时可开始。
2. 卡4 Codex DUT device-login(旧动作保留)。
3. 组合 SHA 通过远端门后,由你批准是否合 main(仍未授权;本轮全部工作不触碰 main)。

## 本轮完成清单(含提交号)
- WN00 `f620edc`:台账/裁决摄取、CI 计费根因留证、SC1 采纳通知(纠正“SC1 缺失”说法)。
- WN01 `4686003`:CT-02/03/04 原子化与不确定态语义、跨 Harness inherit 门、**zcode 真实端口**、458/458。
- WN02 `9736b26`:管理 MCP 目录纠偏(P2/context_mode/transferStatus/最小化/续租有界),461→462 全量。
- WN03 `d7a58a4`:参与者共享桥(去直读 DB、禁自签、grant 复验读、request_key 幂等、HTTP 生命周期),含 stdio/http 全链负测。
- WN04 `70668ff`+`dc64b8d`:受限实例进程级 scope(未知 id 拒启)、MCP 入口与 cloud-kit 打进候选包、操作员卡。
- SC2 `d99cb10`:docs/parallel/shared-mcp-checkpoint.json(bundle 哈希/合同哈希/两侧语义/Linux 必测/剩余代码与宿主阻塞分列)。
- WN06 `6e95922`:组合合并——P02∪WC04 准入并集、双 ring 测试拆分、Linux 压缩后端经 transfer 通道入库
  (协调器保持 W02 两参)、**平台接缝修复:LocalCoreTransport 惰性 endpoint 提供器**(core 轮换后 web 自动恢复,双平台同语义)。

## 风险与诚实声明
- 组合树验证在 Windows 机器完成;Linux 三门在组合 SHA 上的运行是**下一步硬前提**(检查点内已通知)。
- kimi AB/取消覆盖仍未拿到一次全绿(模型间歇;同构建采样与事件关联记录在 remaining)。
- 远端 CI 绿之前,任何“通过”仅指本机/本树证据,不构成发布依据;未合 main/tag/release。
