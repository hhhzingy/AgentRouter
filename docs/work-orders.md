# 实施工作单

## WO-M00-001：需求与平台探针

目标：F01/F05/F22/F23；依赖 Gate：无；输入：原始 V1.0 包。修改 docs、tools、fixtures、native、evidence 和兼容锁。测试：T001/T002/T066 的子探针、私有 Node、数据库加载、Job 进程树与桌面自检。完成条件：原始资料保留、真实版本/哈希记录、探针可重现；干净 Windows 与工具桥真实认证未通过时 Gate 保持阻断。

## WO-M01-001：协议与事务

目标：F08/F09/F10/F23/F25。依赖 M00，现场 Gate 未通过，仅推进独立离线工作。修改 packages/protocol、domain、storage、tests。测试 T017/T018/T026/T071 的协议/身份子集。禁止改变成功静默、指定去向、真实身份、原生屏障。完成条件：离线测试与类型检查有退出码；不把部分检查等同完整功能 Gate。

## WO-M02-001：离线调度与故障

目标：F09—F12/F21/F23。依赖 M01 未完整验收，仅推进模拟/离线部分。修改 runtime、role-bridge、tests。验证幂等 ledger、HELD outbox、可信关联结果、FIFO、恢复隔离、限流和内部 HTTP 边界。证据为本次测试运行记录。模拟测试不计 Harness 支持。

## WO-INDEPENDENT-001：后续独立模块

M03/M04/M05：锁定原生事件解析与 LF 分帧契约；不是完整 adapter。
M06：不可变文件冻结和路径校验子集。
M07：项目登记/归档、桌面只读投影、主窗口/托盘骨架与截图自检。
M08：在线 SQLite 备份、恢复校验、可注入的安全切号状态机与额度未知状态；没有真实凭据替换。
M09：依赖版本/规范哈希审查、可复现命令；未宣告发布完成。
