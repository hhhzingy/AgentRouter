# J2 实施计划与阶段检查点

基线 bbdea2ab86243260c520f5eed791bab90a2b851c；分支 feat/j2-usable-workbench，独立 clone E:/AgentRouter/.local/w11a/integration。包路径 AgentRouter_Codex_J2_可用性收敛执行包，15 文件哈希与结构通过，46 项应用验收初始 NOT_RUN。

J2-0 复现 J1 门禁并建立已知失败回归；J2-1 修正操作状态、稳定身份、异步/分页和待核对记录；J2-2 实现手工角色方案及审阅；J2-3 统一新任务/补充输入及历史；J2-4 四入口和中文渐进展开；J2-5 真实 Electron/Fixture 验收与精确 SHA/CI 记录。每阶段独立提交。

功能保留按执行包 feature-map 映射；协议优先复用，必要稳定身份只通过受限本地桥补充并登记有限 CCR，不重做全量合同。无真实账号/Harness/SSH/Linux，不开放生产组重构/worktree，不合 main，不改原 UI 标签。完成 J2 后停止复核。

当前已知失败测试使用 it.fails 显式标记 J1 缺陷，不计为修复通过：未实现 AI 规划错误启用、同名工作区串项目、无 role 全局事件混入项目、空闲角色灰输入。手工伪造 valid/hash 已源代码定位；真实交互验收必须验证手工表单保存、重载和服务端实体，不能用文本存在替代。

变更清单和每阶段结果续记于本目录 J2 报告；新 UI 和截图写 docs/ui/ui-usability-j2 / evidence/J2，原 UI 基线证据保留。原生对话框人工检查无用户实际记录则 NOT_RUN。

J2-0：基线命令退出码 0，199 项测试、12 项 J1 及 6 项既有 Electron 检查通过；新增 4 个缺陷用例均为 expected fail（已复现，不是已修复）。构建/数据留在项目内，原 J1 截图保留。

J2-1：精确模型/工作区身份、Core 范围分页、读取失败/加载/空记录区分、异步角色隔离、独立租约心跳与250ms事件合并、结构化待核对记录和受限稳定身份已接入。新增4项pending/身份/状态测试及150条项目历史；角色统一新任务用例仍为J2-3预期失败。真实J1 Electron 12项复测通过。后续继续完成编辑器、统一输入及清理交互，不在本检查点宣称J2全部完成。

J2-2：手工组/角色增删改、职责模板、运行目录与权限审阅、严格128KiB JSON校验、导入ID显式映射均已实现。真实Electron完成两组三角色、编辑校验失效、重载不启动、现有组增员及映射导入。role.createFromSpec复用冻结合同，在原事务内保存并更新现有组角色说明名单，初始化和用户暂停分离；重名、权限交集与故障回滚测试通过。内部AI规划仍禁用；不支持YAML，不创建真实worktree。

J2-3：统一任务编辑器支持显式新任务/绑定 task_id 的用户补充、独立草稿、显式结果/交接指令、IME 与重复提交防护。真实 Electron 11 项检查通过，涵盖 FIFO、HELD 原生收尾屏障、150 条安全历史、失败重试、快速切换和事件合并；实际账号/Harness/SSH/Linux 未测。TaskVM 复用既有可选 blockedReason 字段区分等待用户输入与依赖，没有新增合同或迁移。

J2-4：四个项目主入口及旧路由兼容、动态组/角色筛选、持续待处理入口、角色配置抽屉与主对话区、首页3组5头像独立链接、渐进角色配置、本机草稿/旧记录清理已接通。真实Electron15项通过，新增焦点/background inert、导航回退、未知操作改稿/重载/原ID重试、1280×720的125/150/200%截图。人工原生对话框与五分钟用户试用未观察，保持未测。

J2-5最终：源码 1ee22dadc9fb890c44f7e95ff015288306d7a913，217项测试与38项Electron检查通过；两个Windows CI均success。46项验收44 PASS、1 PARTIAL、1人工未测。最终截图采用Electron原生capturePage，早期裁切截图不作为最终证据。无代码工作遗留阻断；人工测试与真实外部环境按约束未测。STOP_FOR_REVIEW。
