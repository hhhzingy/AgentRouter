# 分段压缩与 FULL 占用语义修复

开发父提交：51a3381。

## 改动

- 同一 DeepSeek 后端有界分段；计划完全部请求后再联网，段数/总输入/单请求/最终输出均有限额。
- 每段保持完整 seq/hash 来源集合，失败即停，不自动重试、不用部分成功冒充完整迁移。
- FULL 不再默认 currentUsageTokens=0；仅计划新原生会话时显式为0。已有 ref 且无当前 Driver 使用量时安全阻止，不采用静态 model_json 的旧占用。
- 原 context-migration 测试显式标记新会话0占用，并保留足够最终 envelope 开销；新增预算边界测试继续覆盖超限失败。
- 测试临时文件保留，不执行自动删除。

## 失败分母

分段三项修复前3/3失败，修复后与 Backend 单测合计11/11通过。
FULL预算三项修复前2失败/1通过；修复后相关6文件20/20通过。
旧迁移测试2/2因缺显式新会话使用量前提失败，修正测试前提后本组7文件22/22通过。没有删除测试来换绿。
另补240条、约4.8MB多小条目的离线压力用例；不是1M token实测。最终7文件23/23通过，TypeScript、规范副本及migration/EOL、diff检查通过。

## 未验收

分段目前只取得 OFFLINE 证据，未进行本轮真实API大输入调用。Driver实际占用/恢复探测、preflight切换准备、持久压缩作业/取消、语义质量、1M→256K及原生目标任务仍未完成。预算安全阻止不是上述功能完成。
