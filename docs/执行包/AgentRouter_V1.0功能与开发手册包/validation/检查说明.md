# 文件包检查说明

日期：2026-09-09。本目录记录的是本文件包实际执行的检查，不是 AgentRouter 应用验收。

## 已执行

离线规范检查 36 项通过、0 项失败，详情见 `spec_validation.json`。范围包括 JSON Schema、合法/非法样例、YAML、规范一致性、SQLite 建表、外键、唯一约束和指定事务回滚等。

TypeScript 独立类型声明经过严格类型检查，退出码为 0。它不证明适配器已经实现或真实 SDK 能够连通。

Word 功能手册 16 页、开发手册 25 页均已渲染为页面图像并逐页检查；目录书签未发现缺失目标。最终功能手册重新渲染的页面与已检查版本逐像素一致。详情见 `document_and_type_validation.json`。分页可能随用户本地 Word/字体配置略有变化。

## 未执行

81 项产品验收场景是待实施的计划，全部保持 NOT_RUN。未执行真实 Codex、Kimi Code、pi 调用；未使用真实账号测试切换或额度；未测试 Windows Job Objects、托盘、Electron 打包、实际调度器、断电恢复或安全审计。

本机用于 SQL 声明校验的 SQLite 版本不是 V1.0 生产运行时认证。M00 必须检查打包程序真正加载的版本、兼容性与修复状态。

本包不包含用户密钥、字体文件、可运行 AgentRouter 安装包或已通过现场联调的声明。渲染中间文件不在交付包内。
