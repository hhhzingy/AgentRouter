# 能力真值矩阵与已知缺口（复核者验证清单）

## 能力声明（SC1 fa62a1b，逐条给出复核方法）

| 能力 | 声明 | 复核方法 |
|---|---|---|
| new_ws_direct_transfer | WIRED_TESTED_FIXTURE | vitest tests/integration/context-transfer-engine.test.ts（SH-02 用例：T≥S 且 A=null DIRECT→COMMITTED） |
| new_ws_blank_transfer | WIRED | 同上 SH-03 用例（init 失败→原 ACTIVE 原样） |
| inherit 门控 | SH-01 修复 | vitest tests/unit/context-transfer-decide.test.ts + tests/unit/context-transfer.test.ts（旧双参签名已废除） |
| 同 WS 不暗换 native ref | SH-04 | vitest tests/unit/wc02-continuity.test.ts（save-once vs continuous 对照） |
| preflight NEEDS_NEW_WORKSESSION | SH-04 | 同文件末用例（集成环境） |
| runPrompt 去硬限制 | SH-05 | wc02-continuity 第 2 用例 |
| historical_ws_read_only | 沿用 W02 | tests/integration/v11-w02-ws-readonly.test.ts + role-session.test.ts |
| 远程 Origin 精确匹配 | W-02 | tests/integration/v11-remote-gateway.test.ts WC03 用例（evil 主机名拒/显式 ts.net 收/loopback+端口收） |
| socket 级在途上限 | W-04 | 源码审查 remote-gateway.ts onSocket（inFlightFrames/inFlightBytes 跨 message 累计） |
| 自创建可见持久 | W-05 | 同网关文件 W-05 用例（同 token 重连仍见自创建项目） |
| 配对 scope/controller UI | W-03 | 源码审查 pages-remote.tsx（空 scope=最小权限不放宽） |
| shutdown 分阶段容错 | L-02 同源 | 源码审查 w11-main shutdown（SHUTDOWN_PARTIAL+exit3） |
| StderrRing 共享参考 | L-01 | vitest tests/unit/wc04-shared.test.ts（跨 chunk/EOF/上限） |
| profile 工厂 | WC04 | 同文件（四形状+负测+无密钥字段断言） |

## 已知缺口（不接受"文件已写"式通过）

1. ContextTransfer 真实 harness 通道未接：`transferPorts` 注册表生产侧为空 → 真实 harness inherit =
   显式 CONTEXT_EXPORT_UNSUPPORTED（不静默空白）。zcode warm 通道（export 会话快照 + 首 prompt 种子）是下一接线候选。
2. zcode warm 驻留未做 backend 改造（探针证明协议可行：tools/wc02-zcode-warm-probe.mjs --live，
   同进程两轮依赖 BANANA-7 正确回答）。
3. WC06 组合 SHA 未产出（2 文件冲突未解；冲突预演：`git merge-tree --write-tree HEAD origin/feat/linux-v1.1-port`）。
4. Kimi AB/取消覆盖受模型抖动阻塞（evidence/dut-kimi-flaky-fail-sample.json 为失败样例；
   二分记录：81777d4 PASS / 9796f98 FAIL / 108b70a PASS / fa62a1b 三连 FAIL——同代码通过+失败并存）。
5. 手机/双机 HTTPS 实测未做（候选卡生成依 WC03 修复后的真实 HTTPS 自测，属 WC05/物理门）。
6. Codex DUT BLOCKED_USER。
