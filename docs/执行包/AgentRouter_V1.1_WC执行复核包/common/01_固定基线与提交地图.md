# 固定基线与提交地图（Windows 车道，WC00—WC08）

全部提交在 `feat/v1.1-final-windows-mobile`；每个提交过 pre-commit 三门（C1 代际/冻结源/敏感扫描 findings=0）。

| 提交 | 工单 | 内容 | 上一轮定位项 |
|---|---|---|---|
| 3a8ddfb | WC00 | 事实/保护账本/CI 根因/SC0；撤回旧 W12 卡通杀与裸 HTTP 步骤；DUT 登录迁 `.local-protected` | W-01、X-04、CI 未决 |
| 36d2dbb | WC01 | ContextTransfer 接入公共创建路径；decideTransfer 决策顺序修复；inherit 门控改结构化三输入；新引擎（持久 intent/导出/判定/压缩/初始化/短事务提交/SEEDED 恢复）；协调器派发暂停 | SH-01、SH-02、SH-03 |
| 81777d4 | WC02 | 驱动 continuity 事实字段；backend 不暗换同 WS native ref；preflight NEEDS_NEW_WORKSESSION；runPrompt 去工具硬限制；warm 协议探针 | SH-04、SH-05 |
| 9796f98 | WC03 | Origin 精确匹配；socket 全生命周期在途帧/字节上限；自创建项目可见性持久化（principal 审计）；配对 UI scope/controller | W-02、W-03、W-04、W-05 |
| 108b70a | WC04 | pi 资产仅 pi profile 校验；shutdown 分阶段容错+SHUTDOWN_PARTIAL；StderrRing 负测锁定为共享参考；共享 profile 工厂 | L-01（Windows 参考）、L-02（Windows 侧）、X-01 部分 |
| fa62a1b | WC04 | 发布 SC1（candidate=108b70a，能力真值+provider 模板+Linux 必测清单） | X-01 |
| b2b478f | WC05 | --ab 改 W02 新语义（负验切回拒绝）+MCP 会话命令元数据 | — |

未决/部分完成（诚实清单）：
- WC05 AB/取消覆盖 `PARTIAL_MODEL_FLAKY`（kimi 已知 session/load 后偶发不执行；三点二分 pass+fail 混合）。
- WC06 组合分支进行中：`E:\AgentRouter\.worktrees\v1.1-integration`，冲突=local-native-runtime.ts（4 块）+ stderr-ring 测试 add/add。
- WC07/WC08 未开始（依组合 SHA）。
- Codex DUT 行 BLOCKED_USER（device-login 脚本就绪：tools/login-j3-codex-dut.ps1）。
- GitHub Actions 计费阻塞（BLOCKED_CI_INFRA）：b2c1d5c 起作业未启动，注解为账号付款/消费上限；6298817 及更早双门 success。
