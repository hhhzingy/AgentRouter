# AgentRouter V1.1：WC00—WC08 执行复核包（交由独立 AI 复核）

日期：2026-09-17。本包是"复核后执行包 WC00—WC08"的执行结果，提交给独立复核者审查；不是 release approval。

## 固定审查基线

| 车道 | 分支 | HEAD |
|---|---|---|
| Windows | feat/v1.1-final-windows-mobile | b2b478f0f120eafb2fad54de587ca422183433a4 |
| Linux | feat/linux-v1.1-port | 5f0689f（LC07 收档，与本包无新增交互） |
| 共同祖先 | — | e160ef4ded2cfbd867fcb95500083b2945cf589a |
| 集成预演 | integration/v1.1-cross-platform | 已建 worktree，合并进行中（2 文件冲突未解，见 §WC06） |

上一轮复核（`AgentRouter_V1.1_Windows_复核后执行包`）的全部 SH-01…05 / W-01…05 / X-01…04 定位事项，
本包逐条响应，映射见 `windows/01_WC00-WC08执行明细.md` §映射表。

## 复核方式建议

1. 固定 SHA 读取源码（不信任工作树）：SH 三项修复在 36d2dbb，ZCode 连续性在 81777d4，
   远程加固在 9796f98，共享修复在 108b70a；SC1 检查点在 fa62a1b。
2. 重跑本地门（Windows）：`node node_modules/vitest/vitest.mjs run tests/integration tests/unit tests/contract tests/chaos`
   应为 454/454；聚焦负测见 `scripts/review-checks.mjs`。
3. 真实 DUT（付费路径）不要求复核者重跑；证据取样在 `evidence/`（kimi/zcode→百炼 42 PUBLISHED 各一份、
   kimi 抖动失败样例一份，均无凭据字段）。
4. 裁决请求与输出格式见 `common/03_复核裁决请求与裁决格式.md`。

## 本轮复核者应重点质疑的点（自报短板）

- WC01 引擎的 `initializeTarget` 语义是"seed 提示注入"还是"原生初始化"——当前仅 fixture 端口实现，
  真实 harness 通道未接，inherit 对真实 harness 显式 CONTEXT_EXPORT_UNSUPPORTED。是否接受该中间态。
- WC02 选择了复核给出的替代分支（显式降级）而非 warm 驻留；warm 协议可行性有探针但 backend 未驻留进程。
- WC05 kimi AB/取消覆盖被模型抖动阻塞（三点二分均为 pass+fail 混合，判非代码回归）——请复核该判定依据。
- WC06 合并只完成了冲突定位与 worktree 准备，未出组合 SHA。
