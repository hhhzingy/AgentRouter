# WC00—WC08 执行明细（Windows 车道）

## WC00 — 完成
- CI 根因：b2c1d5c 双门 4 秒失败，注解原文“job was not started because recent account payments
  have failed or your spending limit needs to be increased”→ BLOCKED_CI_INFRA；b2c1d5c vs 6298817
  仅 docs（1 文件 87+）。run id 35071788455/35071788416。
- 保护账本：docs/parallel/wc00-protection-ledger.md（含 ZCode 桌面运行观测、PID 级停止规则）。
- SC0 已被 SC1 取代（fa62a1b）。

## WC01 — 完成（36d2dbb）
文件：packages/core-service/{context-transfer.ts,context-transfer-engine.ts,role-session-extension.ts,
execution-coordinator.ts}。失败先行测试：tests/unit/context-transfer-decide.test.ts、
tests/integration/context-transfer-engine.test.ts。
关键语义：create(context_mode=inherit) 同步段只做门控+intent 落库并返回 {transfer:{op_id,state}}；
引擎经注入的 schedule 在事务外执行；commitTransfer 短事务含 binding.harness 校验与 from==ACTIVE 校验
（竞态→CONTEXT_TRANSFER_RACE）；transferStatus 为只读；request_key 重放命中 command_ledger 返回原句柄
（不重复创建/初始化），提交后世界状态变化的重试以 preflight/revision 差异显式 OPERATION_CONFLICT。

## WC02 — 完成（81777d4）
探针：tools/wc02-zcode-warm-probe.mjs（--live，需百炼凭据文件）。产物结论：warm 可行/未驻留；
现状=每 run 新会话+章程重申+ref 不暗换+preflight 引导新 WS。

## WC03 — 完成（9796f98）
网关 Origin 默认集合在 listen() 时按端口生成（loopback 四种 scheme/host 组合）；
`AGENTROUTER_REMOTE_ALLOWED_ORIGINS` 经 w11-main/launcher/manifest 三处接线。
配对 UI：pages-remote.tsx（kind/controller/scope 三控件）。

## WC04 — 完成（108b70a + fa62a1b）
shared-profile-factory 为纯函数（无 IO、无秘密）；StderrRing 未改实现，仅锁负测
（Linux 采纳即删其本地产物——LC 任务）。

## WC05 — 部分完成（b2b478f）
主任务多 SHA 验证 PASS；AB 新语义 + 取消段代码就绪但被 kimi 抖动阻塞；
evidence/ 含 PASS×2（kimi/zcode 42 PUBLISHED）与 FAIL 样例。
codex 行 BLOCKED_USER；文件产物轮与生产新任务轮未开始（依 AB/取消段通过后进入）。

## WC06 — 进行中
worktree=E:\AgentRouter\.worktrees\v1.1-integration（分支 integration/v1.1-cross-platform，起点 fa62a1b）；
`git merge --no-commit origin/feat/linux-v1.1-port` 已执行，冲突 2 文件：
- packages/platform/local-native-runtime.ts：4 块（Linux P02 selected-harness 准入 vs Windows pi-optional+百炼分支——语义可并集）
- tests/unit/stderr-ring.test.ts：add/add（Linux 测其 host 内实现，Windows 测共享参考实现——拟双方保留，Linux 改名 -linux 后缀，最终以"Linux host 改用共享 ring"收敛）

## WC07/WC08 — 未开始（依组合 SHA）
