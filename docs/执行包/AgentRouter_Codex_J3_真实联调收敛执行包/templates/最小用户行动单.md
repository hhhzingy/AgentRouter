# 最小用户行动单（只在不可自动完成时使用）

关联用例 / 当前源码：
需要你执行的唯一动作：
为什么工具自身无法做（准确错误码/工具限制，不猜原因）：
是否需要管理员 / 独立登录 / 可见桌面：
影响的窗口、目录与持续时间：
明确不触碰的开发进程及凭据：
成功后只返回哪个脱敏结果文件路径：
失败怎么停止/恢复：
等待期间 Codex 继续做哪些不依赖该动作的工作：

不得要求粘贴Key/auth正文；不得附带重新询问预算和SSH。
