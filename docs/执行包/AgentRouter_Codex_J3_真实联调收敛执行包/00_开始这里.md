# AgentRouter J3：真实联调收敛补充执行包

日期：2026-09-11。接收者：Codex，继续单一负责 Core、UI、适配器、安全和调试。UIAI 不再参与。

## 复核基线与证据边界

- 仓库：hhhzingy/AgentRouter；分支：feat/v1-finalization-j3。
- 读取的固定提交：d28aa3e8836b013d53818bcf6475b01c31154710。
- 报告对应受测源码：211abf361c5fb93722a20c41bd98b4bd21592d49。
- 两条该源码 CI：34550830283、34550830431，已查询 success。
- 仓库报告：252 项回归、38 项 Electron 检查（业务是 Fixture）、20 项 Windows 自建进程树测试。不要简单相加成真实功能认证数。
- 已记录一次 DeepSeek 官方 API 联通：9 token；不是 pi 接入。此证据沿用，不重放旧操作。
- 当前真实 Harness 支持数仍为 0。普通 Core 入口仍是 NO_EXECUTOR。
- 本包作者只读取代码、报告、CI 和官方文档；没有运行用户 Windows、没有读取凭据、没有改远端代码。

## 这不是另一轮空泛设计

沿用 docs/j3/work-ledger.json 与原 J3-01 至 J3-12，不重建工程或更换主架构。本包仅修正当前授权、收敛故障诊断、把完成标准从测试数量转成真实端到端证据。

## 本轮执行规则

1. 核对远端和工作树；若比此基线更新，先读差异，禁止重置覆盖。
2. 先修订 H-01 当前状态，旧条款归档，不让互相矛盾的 checkpoint 再触发重复提问。
3. 并行推进两个工程支线：生产执行接线与 Windows 凭据隔离。一个被环境阻断时，继续另一个可做支线。
4. 原则上继续执行已授权动作，不因完成一次离线小 Gate 就停止等待用户说“继续”。
5. 真正需要用户的操作只发一张最小行动单：具体动作、原因、范围、退出条件、结果路径。
6. 未通过隔离的分支不得加载真实凭据；但不阻止写代码、Mock Provider、进程测试等无秘密工作。
7. 先 pi+DeepSeek，再 Kimi Code，再 Codex 单 Harness，随后跨 Harness、UI 与故障恢复，最后独立 Codex 切号/重启。
8. SSH 延后，不询问别名、指纹、Ubuntu、授权目录或 SSH 时间；不能把延后标为 PASS。
9. 不合 main、不发布、不创建正式发布标签，除非后续获得明确批准。

## 交付成功是什么

用户可以在同一个实际 AgentRouter 产品入口中，用真实 Codex/Kimi/pi 完成小任务、交接、取消、恢复；指定结果去向正确；独立 Codex 被测实例可重启并按规则切号；凭据不进入模型可见数据和 GitHub；未测项目有明确状态。

只把 curl 变成 HTTP 200、只重复 252 项 Fixture 回归，均不能关闭本任务。
