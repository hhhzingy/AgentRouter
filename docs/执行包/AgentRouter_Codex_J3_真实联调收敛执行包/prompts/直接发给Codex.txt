请继续 feat/v1-finalization-j3，并读取《AgentRouter_J3_真实联调收敛包》。核对当前远端差异，不覆盖我的工作。

本轮请完成实际生产链路、真实三 Harness 联调、UI 调试、记录和修复，不要只重复 Fixture 回归。
已明确：费用不限；测试仍最低思考、小任务；pi先DeepSeek；Kimi优先K2.7；Codex使用GPT-5.6 Luna系列实际可用模型，按我后续要求使用最低effort；SSH暂缓；业务目录全范围授权但不代表可以读取秘密或破坏已有数据。不要再索取这些已给信息。

我本次重新要求把Codex切号和重启纳入验收，放到最后，只针对AgentRouter拥有的独立被测实例；当前hzxpro开发会话及其认证不改、不登出、不关闭。H-01原EXCLUDED_BY_USER需保留历史并修订为INCLUDED_LAST_DUT_ONLY。不能隔离两个实例或缺独立登录会话时，只挂起该项，不冒险关闭开发进程。

先补J3-01生产注册和六工具、Kimi ACP/pi RPC完整生命周期，同时定位Windows ACL。隔离未过不加载真凭据，但继续所有无秘密工程。不要把canary线程模拟PASS算作完整工具沙箱，不使用全局Full Access绕过失败。

能安全自动执行的步骤连续做完；需要UAC、独立登录或可见桌面时只发具体最小行动单。可以使用当前实际可用的Computer Use测试AgentRouter GUI与原生对话框，但不得打开凭据文件、截取密钥、代批安全授权或控制当前开发应用。

最终给出精确SHA、真实/模拟分别的证据、六条有向交接、故障修复、DUT账号身份与会话连续性、候选包验证、所有未测项。不合main，不发布，不把SSH延后写成通过。
