# 给 Codex 的连续执行指令

继续 feat/v1-finalization-j3，阅读本包，不创建另一套 J4/新大架构。保存原来 J3 test/feature 映射。

## 执行顺序

A. 更新 H-01 和 checkpoint：预算已授权、目录已授权、DeepSeek 优先、低思考、SSH 延后；Codex 切换由本轮新要求纳入最后的独立 DUT。DEV hzxpro 不动。
B. 立即补齐 J3-01/03/04/05 生产注册、配置、进程、工具和事件链，同时定位 J3-02 ACL。普通入口 NO_EXECUTOR 尚未消除前不能说开发完成。
C. 以假数据通过隔离正负控制及假 Provider 试验；无需真实账号就能做的工作继续执行。
D. 通过安全 Gate 的路径先运行 pi+DeepSeek 单体，接着 Kimi、Codex；精确模型由实际能力验证。已通过的原直连 OK 探针不重复冒充进展。
E. 跑六条跨 Harness 有向通信、三角色联合链、取消/断线/恢复、真实 UI与原生对话框。运行中修 bug，保存失败→修复→回归证据。
F. 最后独立监督器执行 Codex 同账号重启、A_TEST→fj→A_TEST 和安全失败用例；缺失第二个独立会话时只挂起这一个用例。
G. 修正发布入口并生成本地候选包，验证它走同一生产路径。SSH 保持延后；不发布、不合 main。

## 不应再发生的循环

- 不再问已经确定的预算、SSH 条件或“pi 先哪个 Provider”。
- 不因一次 ACL 失败重复跑全仓回归后再次发同一个 H-01。
- 不把新增测试数量当真实认证。
- 不为了解除阻断关闭权限检查。
- 不在每个小 Gate 后停止等“继续”；Gate 满足就推进后续已授权动作。
- 需要 UAC/独立登录/前台桌面时发最小行动单，其他无关工作继续。
- 若执行时段或上下文结束，提交明确 checkpoint，列出下一条具体工程动作，而不是宣告 V1.0 已完成。

## 每批次最少汇报

source_sha、tested_sha、CI_sha；实际执行 Harness/版本/Profile代号/模型/effort/endpoint分类；paid_call_count；用例 ID；结果证据；故障复现、根因置信度、修复提交、回归范围；用户需做的最小动作；仍未测试或延后的功能。

## 完成 Gate

实际三家产品路径通过；隔离证据通过；指定消息/文件交接通过；取消和恢复通过；DUT 切号/重启通过或准确登记真实阻断；UI 路径和候选安装包通过。仅 Windows 通过不声明 SSH/Linux 或完整原范围全部通过。

只有阻断链确实需要用户/OS 权限、凭据会话、不可确认副作用或超出授权时才停止该链，且不停止其他能安全推进的工作。
