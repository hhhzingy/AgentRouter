# 合同变更请求（直接用户决策，不提交网页）

CCR ID：待分配。状态：PROPOSED。作者/独立 Reviewer：待登记。

## 要解决的实际问题

给出原功能/Test ID、调用链、最小失败证据。区分旧提案、冻结 schema、实际实现三者；不从历史注释推断当前合同。

## 变更内容

列出准确文件、字段、方法、枚举、错误码及其旧/新含义；是否新增后继 revision；是否涉及数据库迁移和旧客户端。旧冻结 schema 不静默改义，生成类型不手工编辑。

## 安全、兼容与回退

明确身份来源、controller/run scope、幂等、资源/停止、秘密、路径和事件游标影响；提供旧客户端兼容和新客户端负向测试。迁移失败使用哪个备份/旧包，实际回退是否已测。

## 决策与验收

兼容新增按本轮授权，由合同负责人和独立 Reviewer 留痕后实施。破坏性改义、产品范围、安全边界或新费用必须由用户明确决定。记录用户结论、日期、批准范围和生效 source SHA；不把模板写入视为批准。
